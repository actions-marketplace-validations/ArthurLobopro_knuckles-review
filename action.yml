name: "Knuckles Pull Request Review"
description: "Add a Knuckles gif on pull request review"
author: "Arthur Lobo"

branding:
  icon: "message-square"
  color: "red"

inputs:
  type:
    description: "Knuckles Type. Must be `Approved`, `IDK` or `No`"
    required: true
    
runs:
  using: "node20"
  main: "dist/index.js"
